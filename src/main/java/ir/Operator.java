package ir;

/**
 * Arithmetic operator for IR operations.
 *
 * Represents operators used in binary and unary operations.
 *
 * Example usage:
 * <pre>
 * if (op.operator() == Operator.ADD) {
 *     // Handle addition
 * }
 * </pre>
 */
public enum Operator {
    ADD,
    SUB,
    MUL,
    DIV
}
